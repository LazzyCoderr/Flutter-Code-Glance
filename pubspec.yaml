name: flutter_code_image
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5

  # Google font
  google_fonts: ^3.0.1

  # State Management
  mobx: ^2.0.7+5
  flutter_mobx: ^2.0.6+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Code generation
  mobx_codegen: ^2.0.7
  build_runner: ^2.2.0

flutter:

  uses-material-design: true

  assets:
   - assets/
